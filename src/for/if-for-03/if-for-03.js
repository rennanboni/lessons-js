module.exports = {
  /**
   * Retorna lista de numeros distintos
   * Exemplo:
   *   Se entrada for array [1, 2, 5, 5, 8, 8]
   *   retorn será [1, 2, 5, 8]
   * 
   * @param {array} numeros Array com numeros
   * @returns 
   */
  numerosDistintos: function (numeros) {
    return undefined;
  }
};
