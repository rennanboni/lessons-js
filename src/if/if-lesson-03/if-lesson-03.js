module.exports = {
  /**
   * Retorna a soma do dois numeros
   * @param {number} num1 
   * @param {number} num2 
   * @returns 
   */
  soma: function(num1, num2) {
    // modod 1
    //let soma = num1 + num2;
    //return soma;

    // modo 2
    return num1 + num2;

    
    // modo 1
    // if (idade > 18) {
    //   return true;
    // } else {
    //   return false;
    //}

    // modo 2
    // if (idade > 18)
    //   return true;
    // else
    //   return false;
    //}

     // modo 3
     // idade > 18 ? true : false;

     // modo 4
     // return idade > 18;
    
    }
  }
};
