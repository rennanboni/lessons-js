module.exports = {
  /**
   * Cria messagem de boa vindas
   * Verificando o sexo do usuario para
   * Masculino: 'Seja bem vindo <NOME>'
   * Feminino: 'Seja bem vinda <NOME>'
   * 
   * @param {object} usuario Objeto usuario
   * @param {string} usuario.nome Nome do usuario
   * @param {number} usuario.idade Idade do usuario
   * @param {string} usuario.sexo Sexo do usuario sendo 'M' ou 'F' valores
   * @returns Retorna messagem de boas vindas
   */
  criarMessagemDeBemVindas: function(usuario) {
    return undefined;
  },
};
